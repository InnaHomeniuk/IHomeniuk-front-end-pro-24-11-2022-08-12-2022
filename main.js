const hour = +prompt('Уведіть кількість годин', '');
const resultSec = hour * 3600;
alert(`Кількість секунд в ${hour} годинах складає ${resultSec}`);
console.log(`Кількість секунд в ${hour} годинах складає ${resultSec}`);
